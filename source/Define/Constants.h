// ============================================================================
// 定数定義 - ゲーム全体の共通設定
// ============================================================================

#pragma once

#include <DxLib.h>

namespace Game {

// ============================================================================
// スクリーン設定
// ============================================================================

constexpr int DEFAULT_SCREEN_WIDTH = 480;
constexpr int DEFAULT_SCREEN_HEIGHT = 640;

// ============================================================================
// ゲーム更新間隔（フレームレート制御）
// ============================================================================

constexpr int GAME_UPDATE_INTERVAL = 16;  // 約 60fps

// ============================================================================
// プレイヤー設定
// ============================================================================

constexpr float PLAYER_INITIAL_X = 200.0f;
constexpr float PLAYER_INITIAL_Y = 300.0f;
constexpr float PLAYER_SPEED_X = -8.0f;   // X 軸移動速度
constexpr float JUMP_FORCE = 12.0f;        // ジャンプ力

// ============================================================================
// パイプ設定
// ============================================================================

constexpr int PIPE_INTERVAL_X = 300;       // パイプ出現間隔（ピクセル）
constexpr float PIPE_WIDTH = 60.0f;        // パイプ幅
constexpr float PIPE_HEIGHT = 180.0f;      // パイプ高さ
constexpr float PIPE_Y_OFFSET = 240.0f;    // パイプ Y 軸オフセット（中央位置）

// ============================================================================
// 色定義
// ============================================================================

constexpr int COLOR_PLAYER = 0xFFFF0000;    // プレイヤー（赤）
constexpr int COLOR_PIPE   = 0xFF00FF00;    // パイプ（緑）
constexpr int COLOR_BG     = 0xFF1A1A2E;    // 背景（ダークブルー）

} // namespace Game
